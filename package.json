{
  "name": "shopradar-monorepo",
  "private": true,
  "description": "ShopRadar monorepo — extension, website, backend",
  "scripts": {
    "start": "npm run start --prefix shopradar-server",
    "server": "npm run start --prefix shopradar-server",
    "ngrok": "npm run ngrok --prefix shopradar-server",
    "tunnel": "npm run tunnel --prefix shopradar-server",
    "build:sw": "npm run build:sw --prefix shopradar-extension",
    "verify": "npm run verify --prefix shopradar-extension",
    "check": "npm run check --prefix shopradar-extension",
    "check:extension": "npm run check:extension --prefix shopradar-extension",
    "package:store": "npm run package:store --prefix shopradar-extension"
  }
}
